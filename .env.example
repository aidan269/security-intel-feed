# Copy to .env (or export directly in your shell) and fill in.
# .env is gitignored — never commit a real token.

# X (Twitter) API v2 bearer token. Required for the Twitter section.
# Get one at https://developer.x.com → your project → Keys and tokens.
# Recent search requires the Basic tier or higher.
X_BEARER_TOKEN=
